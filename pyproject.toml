[build-system]
requires = ["setuptools>=65", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "dons-cafe-bistro"
version = "0.1.0"
description = "Don's Café & Bistro Web Presence + Catering Bestellsystem"
requires-python = ">=3.11"
dependencies = []
